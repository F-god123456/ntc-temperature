# ThermoGuard-NTC 温度控制器测试文档

**版本：** V1.0 | **日期：** 2026-05-11 | **平台：** STM32F030C8 + NTC + 4G模块

---

## 一、测试环境

| 项目 | 说明 |
|------|------|
| 硬件 | 主控板 + ST-Link + NTC传感器 + 4G模块 + 12V电源 |
| 调试 | 串口助手 (115200,8N1) 查看日志和发送AT指令 |
| 工具 | 万用表、示波器、标准温度计 |

---

## 二、测试用例

### 2.1 温度采集（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 1 | 温度显示精度 | NTC置于已知温度环境，对比数码管和标准温度计 | 偏差 ≤ ±1°C |
| 2 | 传感器故障 | 断开NTC -> 等待30秒 | 显示"ERR"，上传 SensorFault 事件 |
| 3 | 传感器恢复 | 接回NTC | 显示恢复正常，上传 SensorRecover 事件 |
| 4 | 温度超范围 | 短路NTC（ADC≈0）或开路（ADC≈4095） | 传感器错误计数累加，30次后触发故障 |

### 2.2 继电器控制（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 5 | 手动开关 | 按开/关键 | 继电器翻转，状态上传 |
| 6 | 散热模式 | 设置开启>关闭(如30>25)，温度升高至31°C | 继电器开启；温度降至24°C时关闭 |
| 7 | 加热模式 | 设置开启<关闭(如10<15)，温度降至9°C | 继电器开启；温度升至16°C时关闭 |

### 2.3 报警功能（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 8 | 高温报警 | 使能高温报警，温度 > 阈值 | 蜂鸣器响，上传 overTempAlarm 事件 |
| 9 | 报警恢复 | 温度降到 < 阈值-迟滞 | 蜂鸣器停，上传 overTempRecover 事件 |
| 10 | 低温报警 | 使能低温报警，温度 < 阈值 | 蜂鸣器响，上传 lowTempAlarm 事件 |
| 11 | 报警消除 | 报警中发 `AT+SER:eliminateAlarm` | 蜂鸣器停止 |

### 2.4 菜单系统（P1）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 12 | 菜单导航 | 按Menu进入，上下键循环切换 S--1~S--8 | 显示正确 |
| 13 | 参数调整 | OK进入子菜单，上下键调整 | 步进0.1，长按步进1.0，不超限 |
| 14 | 超时退出 | 进入菜单后等待60秒 | 自动退出，参数保存到Flash |

### 2.5 Flash存储（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 15 | 参数保存 | 修改参数 -> 退出菜单 -> 断电重启 | 参数保持 |
| 16 | 恢复出厂 | 调用恢复出厂 -> 读取所有参数 | 全部恢复为默认值 |

### 2.6 AT指令通信（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 17 | 温度上报 | 观察MCU上行数据 | 周期性上报 `AT+PRO:temp,<值>` |
| 18 | 查询属性 | 发 `AT+PRO:?\r\n` | 返回温度、继电器状态、模式等 |
| 19 | 远程开关 | 发 `AT+PRO:switch,1/0\r\n` | 继电器响应 |
| 20 | 远程改参 | 发 `AT+PRO:highMaxTemp,50.0\r\n` | 参数修改并保存 |
| 21 | 非法命令 | 发错误格式AT命令 | 忽略或返回错误，不死机 |

### 2.7 电源管理（P1）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 22 | 主电检测 | 断开/恢复主电源 | 检测并上报 powerStatus 事件 |
| 23 | 电池供电 | 主电断开 | 电池供电约15秒后关断 |

### 2.8 看门狗（P0）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 24 | 看门狗复位 | 停止喂狗（如进入死循环） | 约25.6秒后系统复位 |
| 25 | 正常喂狗 | 连续运行24小时 | 无意外复位 |

### 2.9 边界与异常（P1）

| # | 用例 | 步骤 | 预期 |
|---|------|------|------|
| 26 | 阈值修改时报警状态 | 报警中调高阈值到 > 当前温度 | 报警清除但不上报虚假恢复事件 |
| 27 | 频繁通断 | 快速通断主电源10次 | 系统不崩溃，Flash不损坏 |
| 28 | 连续AT压力 | 每秒1条AT指令，持续30分钟 | 正确响应率100% |
| 29 | 传感器故障+自动模式 | 故障时验证继电器 | 继电器保持当前状态不变 |

---

## 三、测试记录

| 用例编号 | 日期 | 结果 | 备注 |
|----------|------|------|------|
| 1-29 | | □通过 □不通过 | |

**缺陷记录：**

| 缺陷 | 描述 | 严重程度 | 状态 |
|------|------|----------|------|
| | | □严重 □一般 □轻微 | □待修复 □已修复 |

**测试结论：** □通过 □有条件通过 □不通过  
**测试人员：** ________________  
**测试日期：** ________________
